import React from "react";
import ReactDOM from "react-dom/client";

import AuthTestPage from './src/AuthTestPage';

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
      <AuthTestPage />
  </React.StrictMode>
);
